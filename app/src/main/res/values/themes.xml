<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlayAndroid" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary" type="color">@color/white</item>
        <item name="colorPrimaryVariant" type="color">@color/white</item>
        <item name="colorOnPrimary" type="color">@color/white</item>
    </style>

    <style name="Theme.PlayAndroid" parent="Base.Theme.PlayAndroid" />
    
    <style name="MyStyle" parent="Theme.AppCompat.Light.NoActionBar"/>
    
    <style name="MyEditStyle">

        <item name="android:textColorHint">@color/peal_white</item>
        <item name="colorControlNormal"> @color/white</item>

    </style>
</resources>