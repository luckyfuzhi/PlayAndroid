<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="250dp"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_gravity="start"
    android:background="#ffffff"
    android:clickable="true">

    <RelativeLayout
        android:id="@+id/user_frame"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:background="@color/light_blue">

        <ImageView
            android:id="@+id/succeed_login_symbol"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/login_symbol"
            android:layout_centerInParent="true"
            app:tint="@color/white" />

        <TextView
            android:id="@+id/username"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="用户名"
            android:layout_below="@id/succeed_login_symbol"
            android:layout_centerInParent="true"
            android:textColor="@color/white"
            android:textSize="18dp"
            />

    </RelativeLayout>


    <LinearLayout
        android:id="@+id/exit_login"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@id/user_frame"
        android:orientation="horizontal"
        >

        <ImageView
            android:id="@+id/exit_login_img"
            android:layout_width="50dp"
            android:layout_height="match_parent"
            android:src="@drawable/edit_login"
            app:tint="@color/gray"
            android:layout_marginLeft="35dp"
            />


        <TextView
            android:id="@+id/exit_login_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="13dp"
            android:textColor="@color/black"
            android:textSize="21dp"
            android:text="退出登录"
            />

    </LinearLayout>

</RelativeLayout>